/* demo for lexer */
function process($input) {
    // check if value is high enough
    if ($input >= 100) {
        echo "Value: " . $input . "\n";
    } else {
        count := 0;
        while (count < 10) {
            count := count + 1;
        }
    }

    result@ := 123; // this is an invalid token

    return true;
}
